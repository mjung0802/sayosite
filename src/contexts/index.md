# src/contexts/ Index

| File | Summary |
|------|---------|
| `ThemeContext.tsx` | Provides `ThemeContextValue`: `theme`, `cycleTheme()`, `themeLabel`. Wraps useTheme hook in a context for app-wide theme access |
| `TabsContext.tsx` | Provides `TabsContextValue`: `tabs[]`, `activeRoute`, `openTab()`, `closeTab()`, `activateTab()`, `terminalInput` (bool for unsaved dot). Tab state lives here |
