# src/styles/ Index

| File | Summary |
|------|---------|
| `global.css` | Global reset, JetBrains Mono font import, scrollbar styling (dark track/thumb), editor font defaults, focus visibility. Applied once in main.tsx |
| `themes.css` | Three theme definitions as CSS custom properties on `body` class: `dark-plus` (default), `monokai`, `github-light`. Each defines VSCode UI tokens (`--vscode-editor-background`, etc.) and syntax colors (`--syntax-keyword`, `--syntax-string`, etc.) |
